/**
 * Styles for the AttributesDropdown component.
 */

import { makeStyles } from "@fluentui/react-components";

/**
 * Fluent UI styles for the AttributesDropdown component.
 */
export const useAttributesDropdownStyles = makeStyles({
    dropdown: {
        width: "100%"
    },
    optionContent: {
        display: "flex",
        alignItems: "center",
        gap: "8px"
    }
});
